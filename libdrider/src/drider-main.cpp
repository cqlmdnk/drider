#include <drider-main.h>
#include <drider-params.h>
#include <spdlog/spdlog.h>

#include <stdio.h>
using namespace drider;
void DriderMain::drider_start(DriderParams params)
{
	SPDLOG_INFO("Hello from drider lib");
	return;
}
